var guestList = ["Richard", "Dawn","Bailey", "Elliot", "Noah", "Amelia" ];

var guestName = prompt ("What is your name?");

if (guestList.includes (guestName)) {

    alert ("Welcome!")
} else {
    alert ("Your name is not down so you are not coming in!");
}
